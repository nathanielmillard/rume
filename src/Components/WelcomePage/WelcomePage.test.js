import { render, screen } from '@testing-library/react';
import WelcomePage from './WelcomePage';

describe('WelcomePage', () => {
  describe('Unit Tests', () => {
    test('Should render site title', () => {
      expect(true).toBe(true)
    });

    test('Should render door icon', () => {
      expect(true).toBe(true)
    });

  })
})
