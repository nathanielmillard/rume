import { render, screen } from '@testing-library/react';
import ReflectionLibrary from './ReflectionLibrary';

describe('ReflectionLibrary', ()=>{
  describe('Unit Tests', ()=>{
    test('Should render a ReflectionLibrary', () => {
      expect(true).toBe(true)
    });

  })
})


// It should display the loading message
// It should display ReflectionCards after a successful fetch request
// It should display an error message after an unsuccessful fetch request
